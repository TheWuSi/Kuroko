# AGENTS.md

> **核心目标**：交付高质量、安全、可维护的代码。  
> **默认语言**：用户可见输出、文档、注释、Git Commit 默认使用**简体中文**；如仓库规范、外部协作、接口协议或社区约定要求其他语言或格式，以更高优先级约束为准。

## 1. 优先级
- 发生冲突时，按以下优先级执行：**用户明确要求 > 仓库内规范文件 > 外部标准或协议 > 本 AGENTS.md 默认规则**。
- 在已有项目中，优先遵循现有目录结构、编码风格、测试模式和提交规范。

## 2. 硬约束
- 不得提交密钥、令牌、账号口令及其他敏感信息。
- 所有外部输入必须做类型、格式、长度和边界校验；对 SQL、命令、路径、文件、模板渲染、序列化与反序列化等高风险场景采取针对性防护。
- 日志、错误信息和调试输出中禁止泄露敏感数据。
- 新增或修改的关键路径必须进行最小充分验证。

## 3. 默认原则
- 复杂任务先比较方案取舍，再以最小闭环方式实施，避免无关重构和风格性噪音提交。
- 文件统一使用 **UTF-8 无 BOM**，标识符使用清晰英文命名，禁止无意义命名。
- 对复杂业务规则、关键决策和非直观实现写**中文注释**，重点说明"为什么"和业务背景；显而易见的代码不强制加注释。
- 设计优先遵循 **KISS、YAGNI、DRY**；在适用场景下参考 **SOLID**。优先简单、低耦合、易测试的实现，避免过早抽象和为复用而复用。

## 4. 验证与交付
- 按变更风险选择单测、集成测试、契约测试、端到端测试或静态检查，避免无差别全量验证。
- 纯文档或注释变更可不跑测试；若涉及命令、配置、接口示例或运维步骤，至少做可执行性检查或人工核对。
- 交付时说明：做了什么、为什么这样做、如何验证、剩余风险；较大任务再补充技术债或后续建议。

## 5. 检索与工具约束
- 优先依据本地上下文；需要外部信息时按系统检索规则处理。

## 6. 项目导航（Kuroko）

> 以下信息帮助接手本项目的 Agent 快速定位关键资源。

### 6.1 项目定位
Kuroko 是基于 OpenList API 的磁力链接番号管理与离线下载系统。后端 **FastAPI**（`backend/`），前端 **React + Shadcn + Zustand**（`frontend/`），构建使用 **Make**，Docker 部署。

### 6.2 文档入口
| 文档 | 路径 | 用途 |
|------|------|------|
| 需求文档 | `docs/dev/requirements.md` | 全部功能需求 FR-01 ~ FR-11 及非功能需求 |
| 架构设计 | `docs/dev/architecture.md` | 系统架构、分层设计、数据模型 ER 图 |
| API 规范 | `docs/dev/api-spec.md` | RESTful API 完整接口定义与示例 |

### 6.3 关键约定
- **后端分层**：`api/v1/`（路由） → `services/`（业务逻辑） → `models/`（持久化），Schema 定义在 `schemas/`。
- **服务入口**：后端统一以 `python -m app.serve` 启动，自动维护 `config/`、`data/`、`logs/` 目录并编排 Uvicorn。
- **单镜像整合**：根目录 Dockerfile 整合前端静态文件至 `/app/static`，由 `app.core.spa.mount_spa` 统一提供 SPA 路由回退与 API 托管。
- **外部依赖**：OpenList API 封装在 `services/openlist_client.py`；BT 解析封装在 `services/bt_parser_client.py`。
- **API 前缀**：所有接口以 `/api/v1` 为前缀，JWT Bearer Token 认证。
- **构建命令**：`make install` 安装依赖、`make dev-backend` / `make dev-frontend` 启动开发、`make build` 构建生产版。

### 6.4 开发流程
1. 阅读 `docs/dev/requirements.md` 理解需求 → 2. 查阅 `docs/dev/api-spec.md` 确认接口契约 → 3. 参考 `docs/dev/architecture.md` 定位代码位置 → 4. 实现 → 5. 验证 → 6. 提交。